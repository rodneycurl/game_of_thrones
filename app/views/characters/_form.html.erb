<%= form_for [@house, @character] do |f| %>

  <%= f.label :name, "Character Name" %>
  <%= f.text_field :name %>

  <%= f.label :character_photo, "Character Photo" %>
  <%= f.text_field :character_photo %>

  <%= f.submit %>

<% end %>
